# 1. Tipus de sortides en Linux

Quan s’executa una comanda, pot generar dues sortides principals:

| Nom        | Descripció                               | Descriptor |
| ---------- | ---------------------------------------- | ---------- |
| stdout | Sortida estàndard (el resultat correcte) | `1`        |
| stderr | Sortida d’error (missatges d’error)      | `2`        |

💡 Per defecte, totes dues van cap a la pantalla (el terminal).

---

# 2. Redireccionar la sortida d’error

Si vols guardar o eliminar els errors, pots redirigir el descriptor `2`.

## Exemple bàsic

```bash
ls /etc /carpeta_inexistent > sortida.txt 2> errors.txt
```

* `>` → envia la sortida correcta (`stdout`) a `sortida.txt`
* `2>` → envia la sortida d’error (`stderr`) a `errors.txt`

📄 Resultat:

* `sortida.txt` → conté els directoris trobats.
* `errors.txt` → conté l’error “No such file or directory”.

---

# 3. Afegir en comptes de sobreescriure

Pots afegir els errors al final d’un fitxer amb `2>>`:

```bash
comanda 2>> errors.log
```

---

# 4. Enviar errors i sortida al mateix fitxer

Si vols tenir-ho tot junts (errors + resultats), pots fer:

```bash
comanda > tot.log 2>&1
```

Significa:

* `>` → envia `stdout` a `tot.log`
* `2>&1` → envia `stderr` cap al mateix lloc que `stdout`

---

# 5. `/dev/null` — la paperera del sistema 

`/dev/null` és un dispositiu especial que descarta qualsevol cosa que li enviïs.
S’utilitza per eliminar sortides o errors que no vols veure ni guardar.

## Exemples

* Descartar errors:

  ```bash
  ls /carpeta_inexistent 2> /dev/null
  ```

  ➜ No es mostra cap error.

* Descartar tota la sortida (correcta i d’error):

  ```bash
  comanda > /dev/null 2>&1
  ```

  ➜ No es mostra res, com si no hagués passat.

---

# 6. Resum visual

| Objectiu                 | Comanda                    |
| ------------------------ | -------------------------- |
| Només guardar la sortida | `comanda > sortida.txt`    |
| Només guardar els errors | `comanda 2> errors.txt`    |
| Guardar tots dos junts   | `comanda > tot.txt 2>&1`   |
| Ignorar errors           | `comanda 2> /dev/null`     |
| Ignorar-ho tot           | `comanda > /dev/null 2>&1` |

---
